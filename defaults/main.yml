---
# Define the version of Apache Karaf to download
karaf_version: 4.0.5

# Define URL to download Apache Karaf
karaf_url: http://archive.apache.org/dist/karaf/{{ karaf_version }}/apache-karaf-{{ karaf_version }}.tar.gz

java_home: /usr/lib/jvm/java

java_xms: 512

java_xmx: 512

karaf_home: /usr/local/karaf

karaf_user: karaf

# Define list of Karaf features to be installed
#karaf_feature_install:
#  - "feature:repo-add feature_repo1"
#  - "feature:install feature1"
#  - "feature:install feature2"
#  - "feature:install feature3"
